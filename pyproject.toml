[project]
name = "barricades"
version = "0.1.0"
description = "Quoridor game core and engine"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "torch",
    "numpy",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
